# dice-assets

Store backend for the **Dice** iOS app's Pack Gallery — cosmetic and content
packs served straight from this repo (`raw.githubusercontent.com`, push to
`main` = live). See `GALLERY_HANDOVER.md` for the full integration guide and
`IAP_METADATA.md` for the App Store Connect product sheet.
